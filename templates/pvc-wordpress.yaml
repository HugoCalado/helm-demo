apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: wp-pv-claim
  labels:
    app: {{ .Values.appName }}
spec:
  accessModes:
    - "{{ .Values.volume.wordpress.accessMode }}"
  resources:
    requests:
      storage: {{ .Values.volume.wordpress.size }}
